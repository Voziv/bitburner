import { NS } from '@ns';

const WORLD_DAEMON = 'w0rld_d43m0n';

export async function main(ns: NS): Promise<void> {
    if (ns.serverExists(WORLD_DAEMON) && ns.hasRootAccess(WORLD_DAEMON)) {



        ns.getBitNodeMultipliers()
        ns.singularity.destroyW0r1dD43m0n(4, 'init.ts');

    }
}